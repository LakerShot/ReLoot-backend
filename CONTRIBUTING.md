# Contributing to ReLoot Backend

Спасибо за интерес к проекту! Мы рады любому вкладу.

## 🚀 Начало работы

1. Форкните репозиторий
2. Клонируйте свой форк: `git clone <your-fork-url>`
3. Установите зависимости: `npm install`
4. Создайте ветку для фичи: `git checkout -b feature/my-feature`

## 📝 Процесс разработки

### Структура коммитов

Используйте [Conventional Commits](https://www.conventionalcommits.org/):

- `feat:` - новая функциональность
- `fix:` - исправление бага
- `docs:` - изменения в документации
- `style:` - форматирование кода
- `refactor:` - рефакторинг кода
- `test:` - добавление тестов
- `chore:` - обновление зависимостей и конфигурации

Пример:
```
feat: add user profile endpoints
fix: correct JWT token validation
docs: update API documentation
```

### Стандарты кода

Проект использует:
- **ESLint** для проверки качества кода
- **Prettier** для форматирования
- **TypeScript** strict mode

Перед коммитом:
```bash
npm run lint
npm run format
npm run test
```

### Добавление новых фич

1. **Создайте модуль** с помощью NestJS CLI:
   ```bash
   nest g module feature-name
   nest g service feature-name
   nest g controller feature-name
   ```

2. **Добавьте модели в Prisma** (`prisma/schema.prisma`)

3. **Создайте миграцию**:
   ```bash
   npx prisma migrate dev --name add_feature_name
   ```

4. **Напишите DTO** с валидацией (class-validator)

5. **Добавьте документацию Swagger** (@ApiTags, @ApiOperation)

6. **Напишите тесты** (unit и e2e)

### Pull Request

1. Убедитесь, что все тесты проходят
2. Обновите документацию при необходимости
3. Создайте Pull Request с описанием изменений
4. Дождитесь ревью

## 🧪 Тестирование

```bash
# Unit тесты
npm run test

# E2E тесты
npm run test:e2e

# Покрытие
npm run test:cov
```

## 📚 Документация

- Обновляйте README.md для крупных изменений
- Добавляйте JSDoc комментарии к публичным методам
- Обновляйте WARP.md при изменении архитектуры

## ❓ Вопросы?

Создайте issue с меткой `question`

## 📄 Лицензия

Внося вклад, вы соглашаетесь, что ваш код будет лицензирован под MIT License.